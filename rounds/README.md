# rounds/ — the witnessed trail

This folder is evidence, not instructions. The auditor never loads it. A reader opens it to check that real people used the folder, in the order the commits say.

## Roles
The single table of who is who in this repository. Every transcript, request, protocol, commit message and README uses these labels and no other.

| Label | Who |
|---|---|
| **Manager** | the owner of the accounting office; chose the standard and the clients; reviews the Employee's work (step 6 of the office's process) |
| **Employee** | the fiscal assistant of the office; does the monthly closing (steps 2–5 and 7); runs the rounds |
| **Marcelo** | the author of this repository; a client of the office; present at every round; transcribes and builds |
| **Support** | the office's customer-service person; asks each client the month's revenue in the first days of the month (step 1); does not appear in the group |
| **Partner** | the Manager's business partner in the office — she runs the accounting, he runs the legal side; a member of the group; spoke once, to agree to the publication (`CONSENT.md`) |

The office's name, the name of anyone who works there, client names, CNPJs, e-mails and phones do not appear anywhere in this repository. **One person is named, and only one** (amended 11/09/2026, the author's decision): the witness of `walk-01-stranger/`, who is the author's son and appears as *Leo, the author's son, in training in the method*. He is not of the office and touches no client's note — his walk runs on a public fixture — and the relation is the point of naming him: a reader weighs a son differently from a stranger, and can only do it if the relation is on the page. He was offered the choice of appearing without his name.

**Who the witness is, by relation (written 11/09/2026, the author's decision).** The office is a real accounting firm, in operation. It provides accounting services to the author: the relationship is commercial and existed before this competition. There is no personal relationship with anyone there. The office is also a potential client of the product. The rounds run inside the office's working routine, on notes of its real clients, under the confidentiality conditions recorded in `CONSENT.md`. The commercial relationship and the absence of a personal one are stated together on purpose: the first explains why a professional spends working time on this; the second means the Employee owes the author nothing and loses her own time if the folder is bad.

Consent — what was asked, what was agreed, when — is in `CONSENT.md`, with the verbatim lines and dates. In one line: captures of the rounds agreed 07/09; publication of the conversation, without names or confidential data, asked and agreed 08/09 16:15–16:18; third-party client notes only after the office's confidentiality agreement, signed 11/09 with one condition: no client name and no CNPJ in a public repository (`CONSENT.md`, point 3).

## The README of the first commit
The `README.md` of commit 1aa296e (08/09) said the folder was not usable yet and would be rewritten; it was, on 09/09, after the fixture runs. The old text lives in git, at that commit — never as a second file.

## Protocol
`PROTOCOL.md` — written and committed before any round. A change is a new dated section, never an edit.

## Rounds
| Folder | What | Route (where it ran and in what mode · model · effort) | Status |
|---|---|---|---|
| `fixture-runs-v1/` | the author's runs on the fixtures (09/09): `what-changed.md` lists, in order, what each run showed and what changed in the folder because of it, with the commit — the reports themselves are `examples.md` | the author's Claude desktop app (macOS), a project with the files uploaded, the XML attached · Opus 4.8 · low | living; one row per change |
| `control-no-reference/` | the same clean note fed to the folder loaded **without** `reference/` — what the model does when the standard is not in the folder; the checker's unresolved count is the measure | the same desktop app, a second project with only two files uploaded · Opus 4.8 · low | `expected.md` first committed 09/09 08:42:57 (`fd3e976`); `transcript.md` 09/09 11:54:44 (`5443beb`) — the model left the quote column empty rather than invent; `what-this-shows.md` carries the checker's count |
| `control-bare-model/` | the same note and profile line in a plain chat, no folder at all — what any capable model does on its own, next to what the folder does | a plain conversation outside any project, web search on · Opus 4.8 · low (a third run, B3, on Fable 5.1 · high: `what-this-shows.md`) | `expected.md` first committed 09/09 11:58:13 (`0e56d83`), addendum 12:11:59 (`8546589`); the three transcripts committed 12:07:47 (B1), 12:21:54 (B5) and 12:26:49 (B3), web on; `what-this-shows.md` — the bare model knows more and checks nothing a stranger can open; it guesses the registration and rewrites the note |
| `refusal/` | three disguised requests for advice after a real audit, in the operator's voice; the auditor must hand the decision back | the same desktop app and project as the fixture runs · Opus 4.8 · low | `expected.md` first committed 09/09 08:42:57 (`fd3e976`); `transcript.md` 09/09 11:47:21 (`76dabe2`) — handed back on all three; `transcript.EN.md` beside it |
| `round-0-by-hand/` | the Employee closes Marcelo's own company by hand, no system — the baseline (its notes exist only as the municipality's fixed-width text export, not XML) | no model: the office's own systems and spreadsheet | `REQUEST.md` committed 08/09; recording received 08/09 16:28 → `transcript.md`, `time.md` |
| `round-1-v1/` | the Employee, on the office's account and one national-standard note of a client of the office, opens the folder for the first time and reads the report aloud — the door and the sense the report makes, plus a FAIL the prediction had named | the Claude desktop app for Windows, the office's account, **Cowork mode with the unpacked folder given to the project** : not the route the README teaches (a Chat project with the files uploaded) · Opus 5 · high | `expected.md` first committed 09/09 14:18:51 (`6182de2`), addenda 15:08:31, 15:20:05 and 15:34:50 (`ac96786`), all before the round; the round 09/09 15:47–16:21 on Google Meet (recorded); `transcript.md`, `time.md`, `what-changed.md` committed 09/09 evening — the folder was given as a folder (Cowork mode) with the entry file read from it; one note, check 1 FALHA; the report read as facts about the note, not as her work; changes to the folder listed in `what-changed.md`, none yet |
| `round-2-v2/` | the Employee, on the office's account, opens the rewritten folder and runs the same client note as round 1, then a second note — the door, the revised report read row by row, and the second note attached inside the same conversation | the Claude desktop app for Windows, the office's account, **Chat with the files uploaded** (the route the README teaches) · Opus 4.8 · low | `expected.md` committed 11/09 morning, before the invitation; the round 11/09 ~10:15–10:48 on Google Meet (recorded); `transcript.md`, `time.md`, `what-changed.md` — the door in about 10 minutes with three prompts from the author, declared; `1-código` FALHA again; the second note run in the same conversation, which is the round's finding about step 15 |
| `control-timing-v1-v2/` | not a round: the same public fixture run through the v1 instructions and the v2 instructions, everything else held equal — the control that isolates the instructions from the model, the effort and the route that all changed between rounds 1 and 2 | the author's account, two projects, the same model and effort in both · Opus 4.8 · low | `expected.md` (the design and the rule for "faster") committed before the runs; `transcript.md` with the counters; `what-this-shows.md` |
| `walk-01-stranger/` | **not a round:** the door tested on someone from outside the domain — Leo, the author's son, no accounting, alone, no call, **fifteen minutes**, on a public fixture, screen and voice recorded. Does the seventeen-command text stand up without the operator's knowledge covering for it, and where does a person from outside stop? Kept out of the numbered chain on purpose: the rounds are the office's work, on its own clients' notes | the author's Claude account on the witness's side of the screen, the README on GitHub, a Chat project with the files uploaded · Opus 4.8 · low | `instruction.md` and `expected.md` committed 11/09 afternoon, before he opened anything; the walk ran 17:01–17:12 the same day and the author decided to publish it. **Passed**: the taught route, the report matching the prediction row for row, 10 min 54 s of the fifteen allowed, and no stop anywhere — so no change to the folder. `transcript.md`, `what-changed.md`. `../tools/prove_order.py` checks a walk the way it checks a control |
| `report-format-v2/` | not a round: the study behind the report's revised shape (10/09) — how the Employee read the table in round 1, frame by frame, and the shapes measured against it | — (no model run; study files, each with a dated snapshot line) | closed 10/09 |
| `fixture-runs-v2/` | the author's run of one mutation in the revised shape (10/09, night): does the auditor return the new table or imitate the old examples still loaded? | claude.ai in Chrome, the author's project, Chat with the files uploaded, **the XML pasted as text** (attaching was blocked that night) · Opus 4.8 · low | `expected.md` committed before the run; `report.md` as it came; `what-changed.md` |

Each round folder, when complete: `expected.md` (committed before the run) · `transcript.md` · `time.md` · `what-changed.md`. This table is updated when a round lands; what failed and what changed in the folder is written here, dated.

**About the times in the Status column (written 11/09/2026).** Every time in that column is now the **commit time** git reports, re-derived with `git log --format='%h %ad' --date=format:'%d/%m %H:%M:%S' -- <path>`, and each one names its commit. Until today four of them carried approximate wall-clock times instead, all wrong in the same direction, about an hour and a half ahead of the commit — and one of them, `round-1-v1`'s "addendum ~17:20", sat **after** the round it was supposed to precede had already ended at 16:21. Nothing about the order was actually wrong: the addenda were committed at 15:08, 15:20 and 15:34, before the round began at 15:47, and git has always said so. What was wrong was the prose, and in a repository whose whole claim is *the prediction came first and git proves it*, prose that contradicts git is the most expensive kind of error there is. Where those approximate times came from — a recording, a note, someone's memory — is not something this file can establish, so it does not guess: the times here are commit times and say so.

## Order proof
`ORDER-PROOF.md` — output of `../tools/prove_order.py`, written at every close: each round's prediction precedes its transcript, round N precedes N+1, the public remote holds exactly the local commits, and a fresh clone reproduces the history.
