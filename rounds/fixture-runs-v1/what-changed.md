# Fixture runs of v1 (09/09/2026) — what each run showed, and what changed in the folder because of it

The author ran the folder on its own fixtures in a Claude project (conditions in `examples.md`, per example). Predictions were committed before each run (`expected/`, commit 760eaa0 and later); the reports are in `examples.md` exactly as returned. This file is the single list, in order, of what a run showed and what was changed after it — each change in its own commit, the report never edited. For the rounds with the office, the same list lives in `round-N-*/what-changed.md`.

| # | Run | What it showed | What changed | Commit |
|---|---|---|---|---|
| 1 | Example 1 (nfse-01, clean) | On check 2 the auditor quoted the layout's field description ("Valor líquido da NFS-e."), verbatim from `reference/tables/required-fields.md`; the checker only accepted quotes from the law texts and flagged it. The auditor was right; the tool was narrower than the rule. | `rules.md` § 3: a check-2 row quotes the layout's description verbatim from `required-fields.md` (or `—`). `check_audit.py` C1 accepts it for check-2 rows only. | 72ed913 |
| 2 | Example 1, English block | Every English quote resolved verbatim in `reference/en/` — looked up, not translated. Nothing to change. The header first said the English ran "with the new rules.md"; it is a rendering of the same report, not a new audit — corrected. | Header of the English block in `examples.md`. | 9697ece |
| 3 | Example 2 (m5: vISSQN 8.00 → 12.00) | Predicted FAIL on `5-aritmética`; the auditor returned PASSA with the implied rate (3,00%) as informational. The note has no rate field, so the rule as written had nothing to multiply the base by. **The prediction was wrong, not the run.** | `rules.md` check 5 says so in one clause (a wrong `vISSQN` on a note without a rate field is not detectable by this check; `vLiq` = `vServ` − deductions − retentions always is). `expected/m5-aritmetica-iss.md` corrected to what the rule supports, the wrong version kept in git and named in `CHANGE.md`'s addendum. New mutation `m6-valor-liquido` (vLiq 400.00 → 380.00) tests the checkable half, prediction committed before its run. `check_audit.py` C3 lets a mutation declared "not caught" in its `CHANGE.md` stand without a FAIL row. Author's decision "a", 09/09. | 08876fc |
| 4 | Example 2 | The auditor cited the layout for check 2 by its full path (`reference/tables/required-fields.md`); C2 only knew the short form. | `check_audit.py` C2 accepts the full path. | 5d65ce6 |
| 5 | Example 3 (m6: vLiq 400.00 → 380.00) | The FAIL came on the right field. Severity: predicted "bloqueia o fechamento", returned "corrigir antes de fechar" — the auditor read § 3 by consequence (a wrong net value does not change the tax) and was right. | `rules.md` § 3 names the case in each severity class; `expected/m6-valor-liquido.md` corrected, earlier version in git. Author's decision "3a", 09/09. | 78965ed |
| 6 | Example 3, English block | The auditor left the Portuguese quotes in the English report and said why ("the Portuguese is the standard"). Against `rules.md` § 3 and `identity.md`; the checker reported five unresolved quotes. The answer is kept as it came, inside `kept-as-came` fences the checker reports without gating. | `rules.md` § 3: the English report quotes `reference/en/` always; the note in `INDEX.md` is not a reason to keep the Portuguese. A second English answer was requested in the same conversation and is appended after the first. | 0f1ff42 |
| 7 | Example 4 (nfse-02 without the profile line — the null case) | Three divergences of one kind, the auditor supplying what the profile did not: answered in English to a message with no text; `1-código` PASS because the code exists in ANEXO B (with "not verified" in the location); `5-alíquota` NOT APPLICABLE from the note's own flag and free text. Check 4 correct. | `rules.md`: § 3 — a message with only files is Portuguese; check 1 — no profile line → `1-código` CANNOT DETERMINE even when the code exists; § 0 — the note's flags and free text do not replace the profile line; without it `1-código`, `4`, `5-alíquota` are CANNOT DETERMINE. Re-run as example 4b with the corrected file. | f38ec00 |
| 8 | Example 4b (null case, re-run) | Matches the prediction row by row. Nothing to change. | Decision (author, 09/09): from example 5 on, the English twin is a rendering by the author's session (quotes looked up in `reference/en/`, checked by C1), not an answer of the auditor — the auditor's own lookup was already shown in examples 1, 3 and 4b. Said in the opening of `examples.md`. | this commit |

Also found while loading the project, before any run (not from a report): a Claude project refuses `.zip` and reads `.xlsx` poorly → the official annexes shipped as CSV, the XSD unpacked (3ea3db4); the project flattens paths, so `pt/excerpts/x.txt` and `en/excerpts/x.txt` collided → English excerpts renamed `*.en.txt` (074accd); the placeholder README of the first commit says the folder is not usable yet, so it was not loaded for these runs (the README that replaces it is written after the examples).

**What these runs do not prove.** They were fed by the author, on anonymised notes of the author's own company, with the profile line the author wrote. They show what the folder returns on inputs whose answer was written down first; they do not show a person from the office using it — that is what the rounds in `round-*/` are for.
