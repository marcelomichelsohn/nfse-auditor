# Round 1 — expected (written 2026-09-09 ~15:30, before the round; committed before `transcript.md`)

**Who.** The Employee (the office's fiscal assistant), alone, on the office's paid Claude account, in a Claude project she creates herself (model Opus, high effort, as the README tells her) from a folder the author sends — the loaded files only (`README.md` § "O que carregar"), unpacked, plus a short Portuguese guide to create the project and paste `CLAUDE.md` into its instructions. She does not receive the repository link: a clone would carry `expected/`, `rounds/` and `fixtures/` into her project. Round 1 is her first contact with the folder (`PROTOCOL.md`, amendment 2).

**What she does.** She chooses notes of the office's clients — some she knows are wrong, some right, old ones allowed — in the national-standard XML (the office's fiscal software downloads them from the national portal), writes the client's profile line from her control spreadsheet, feeds them one per conversation (as the README says and as the folder was tested), and says what stuck and whether the report makes sense to her. Screen and voice recorded by her; transcript with roles only.

**What we expect to see, written down first.**
1. **The door.** Most likely trips: creating the project and uploading the files (about forty of them; the `official/csv/` folder is the bulk); pasting `CLAUDE.md` into the instructions rather than uploading it; composing the profile line from the spreadsheet (which column is "anexo", which is "item LC 116"); attaching the XML instead of pasting it. We expect at least one of these to stop her before the first report. Measure: minutes to the first report, in her words, and what she asked or did not find.
2. **The notes.** If a note is the municipality's own XML (root `CompNfse`) rather than the national standard (root `NFSe` with `DPS`), the report lists it under "Não lidos" and audits nothing — the folder says so in the README's third stop; we expect this to happen at least once, because the office issues through the municipality's site today, and it is a finding about the office's data flow (the note that does not reach the national portal), not about her.
3. **The reports.** On national-standard notes with a profile line: one table per note, the six checks in order; check 1 fails where the code is not the client's item (the wrong-code note she said exists); check 4 returns CANNOT DETERMINE where the note's `regApTribSN` says 1 and the profile says fixed ISS (the office confirmed on 09/09 the municipal site cannot set that field); check 5 NOT APPLICABLE on the rate for a Simples optant; check 6 NOT APPLICABLE. On the export-of-services note (foreign taker), the folder has no check for LC 116 art. 2 I yet: we expect the auditor to either say so (CANNOT DETERMINE with the boundary named) or miss it — either outcome goes into `what-changed.md` and the candidate check.
4. **Her judgement.** Where she knows a note is wrong and the auditor passes it, that is the measure that matters; where the auditor flags something she did not know, we record whether she agrees.
5. **Time.** Her total, in her words, dated, with what it does not prove (a first use, on notes she chose, on a different task from round 0).

**What is committed before she starts:** this file; the README of 09/09 (its Portuguese block, the door she reads, is 436 words at commit time — the measure for any rewrite after the round); `examples.md` with three worked examples. **What is not touched during the round:** the five files. Fixes go in `what-changed.md` after her transcript, each in its own commit, and a second run is `round-1b-*/`.

**What this round will not prove.** One operator, one office, one afternoon; notes she chose; a first use. It does not measure time against round 0 (a different task) and it does not prove the checks are the right ones — her wrong notes are the test of that.

## Addendum — 2026-09-09 ~17:20, after the office sent its notes and before the round

The Employee sent four notes of one client, in the national standard (root `NFSe`, `DPS` embedded, signed), all of August 2026, all with the same service code, all issued by a Simples optant with no rate field on the note. The files stay private until the office's confidentiality agreement; nothing of them is in this repository. What is written here uses no client data: note numbers in the emitter's sequence, and what each check should return.

Prediction per note (she composes the profile line from her spreadsheet; we do not know it):
- **All four:** read as national-standard XML (not "Não lidos"). Check 2 PASSA. Check 3 PASSA (incidence at the provider's establishment; the item is not among the exceptions of LC 116 art. 3). Check 4 PASSA if her profile says Simples optante with a numbered annex; CANNOT DETERMINE if it says fixed ISS (the `regApTribSN` tension the office confirmed on 09/09). Check 6 NÃO SE APLICA.
- **Check 1 depends on her profile's item.** If the item she registers is the one on the notes, PASSA; if not, FALHA on `1-código` — the office said one client issued with the wrong code last month. This is the row her judgement measures.
- **Three of the four notes** carry an implied ISS of 2% of the value; **one (the third in the sequence) carries 5%.** The prediction that matters: on that note the auditor must return `5-alíquota` NÃO SE APLICA (a Simples optant pays the ISS inside the DAS; the municipal table is not the applicable rate) and report the implied rate as informational — **not FALHA**. That is failure mode 4 of the spec ("false FAIL from a table that does not apply") on a real note for the first time. `5-aritmética` PASSA on all four if `vLiq` adds up.
- **Where she may trip:** composing the profile line (which spreadsheet column is the item; whether the annex is a number or "fixo"); the model choice (Opus, high); attaching one note per conversation.

If the round shows the 5% note as a FAIL, the rule held on the author's fixtures and broke on a real note; the fix goes in `what-changed.md` after the transcript, in its own commit.

**17:35, before the round — the Employee answered the profile question:** the control sheet has every field of the profile line except whether the client exports. Fix before the round, in the README's step 6 and `rules.md` § 0: the `exporta` part is optional; no check in this version needs it (the export-of-services check stays a candidate, untested in this round). Door after this change: 446 words in the Portuguese block.
